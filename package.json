{
  "name": "sgl-landing",
  "version": "1.0.0",
  "description": "SGL Landing Page with Environment Variables",
  "main": "index.js",
  "scripts": {
    "setup": "copy env.example .env",
    "dev": "npm run build && node -e \"require('dotenv').config(); require('live-server').start({port: process.env.PORT || 3000, root: './dist', open: '/sglLanding.html'})\"",
    "build": "node build.js",
    "start": "npm run build && node server.js",
    "serve": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
} 